[build]
builder = "DOCKERFILE"
dockerfilePath = "./Dockerfile"

[deploy]
startCommand = "sh -c 'alembic upgrade head && exec python bot.py'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
